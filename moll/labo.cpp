#include <iostream>

int main() {
    int s = 0;  // Initializing the variable 's' with the value 0
    std::cout << "Initial value of s: " << s << std::endl;
    
    // Performing some operations using the variable 's'
    s = 10;  // Assigning a new value to 's'
    std::cout << "Updated value of s: " << s << std::endl;

    return 0;
}
